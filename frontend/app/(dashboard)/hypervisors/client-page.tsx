"use client"

import ProxmoxSettingsPage from "@/app/(dashboard)/settings/proxmox/client-page"

/** `/hypervisors` is the Proxmox screen's address; the component lives with the other settings pages. */
export default function HypervisorsPage() {
  return <ProxmoxSettingsPage />
}
