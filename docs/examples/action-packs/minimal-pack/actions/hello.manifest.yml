key: hello
name: Say hello
description: >-
  Prints a greeting on the target host. Useful as a smoke test that
  LabDog can reach the host over SSH and that the pack is loaded
  correctly.
icon: Zap
playbook: hello.yml
version: "1.0"
estimated_duration: "10 seconds"
# Safe: nothing is modified, no snapshot needed.
destructive: false
# Runs equally on a single host or every member of a group.
supports_group: true
supports_host: true
# No user-facing parameters -- the action has none.
parameters: []
