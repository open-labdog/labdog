Task 8: Command Palette with cmdk
===================================

## Files Created/Modified
- [CREATED] frontend/components/command-palette.tsx
- [MODIFIED] frontend/components/app-shell.tsx (added CommandPalette import + render)

## Checklist
- [x] frontend/components/command-palette.tsx created
- [x] Opens with Cmd+K (Mac) / Ctrl+K (Linux/Windows) — keydown listener on metaKey||ctrlKey + "k"
- [x] Shows all nav items: Dashboard, Groups, Hosts, SSH Keys, Git Repos, Audit Log, Users
- [x] Search input filters items by name — cmdk built-in filtering via Command.Input
- [x] Selecting item navigates to that page and closes palette — handleSelect uses router.push + setOpen(false)
- [x] Closes on Escape — Dialog handles Escape natively via @base-ui/react Dialog
- [x] <CommandPalette /> added to frontend/components/app-shell.tsx — rendered after main layout div
- [x] rtk tsc --noEmit exits 0

## Type Check Output
$ rtk tsc --noEmit
TypeScript compilation completed (exit 0)

## Implementation Notes
- Uses cmdk Command (not Command.Dialog) to avoid Radix Dialog conflict
- Wrapped in project's @base-ui/react Dialog component
- Navigation-only — no entity search, no mutations, no sub-commands
- All 7 nav items hardcoded (Dashboard, Groups, Hosts, SSH Keys, Git Repos, Audit Log, Users)
- data-[selected=true] attribute styling for keyboard navigation highlight
