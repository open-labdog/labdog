T7 - Resolver Sync Celery Task + API Endpoints
================================================

Files created:
- backend/app/tasks/resolver_sync.py — Celery task `run_resolver_sync`
- backend/app/api/resolver_sync.py — 4 API endpoints (plan, sync, group-sync, job-status)
- backend/app/main.py — registered resolver_sync_router

Verification output:
=== Resolver sync routes ===
/api/resolver/groups/{group_id}/sync
/api/resolver/hosts/{host_id}/plan
/api/resolver/hosts/{host_id}/sync
/api/resolver/jobs/{job_id}

=== Task import check ===
Task name: app.tasks.resolver_sync.run_resolver_sync
Task queue: long_running

=== API import check ===
Router prefix: /resolver
Router tags: ['resolver-sync']
Endpoint count: 4

=== App import check ===
$ cd backend && source .venv/bin/activate && python -c "from app.main import app; print('OK')"
OK

All imports and registrations verified.
