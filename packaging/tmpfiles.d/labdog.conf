# systemd-tmpfiles configuration for LabDog.
# Creates /run/labdog at boot with the right ownership.
# Vendor path: /usr/lib/tmpfiles.d/labdog.conf
# (Admin overrides go in /etc/tmpfiles.d/labdog.conf.)

d /run/labdog 0755 labdog labdog -
