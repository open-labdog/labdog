Task 8: Resolver Drift Detection + API
=======================================

Files created/modified:
- backend/app/tasks/resolver_drift.py (NEW)
- backend/app/tasks/__init__.py (modified - added route)
- backend/app/api/resolver_sync.py (modified - added 2 endpoints)

Celery Tasks:
- run_resolver_drift_check(host_id) - single host drift check via SSH
- check_all_resolver_drift() - periodic task for all drift-enabled hosts
- RedBeat schedule registered for periodic execution

API Endpoints:
- POST /api/resolver/hosts/{host_id}/drift-check
  Collects actual resolver state via SSH, compares with effective config,
  updates HostModuleStatus.sync_status and last_drift_check_at
- PUT /api/resolver/hosts/{host_id}/drift-settings?enabled=true
  Toggles drift_check_enabled on HostModuleStatus for resolver module

Verification:
$ cd backend && source .venv/bin/activate && python -c "from app.main import app; print('OK')"
OK

$ python -c "from app.tasks.resolver_drift import run_resolver_drift_check, check_all_resolver_drift; print('Tasks OK')"
Tasks OK

Pattern: Follows service_drift.py exactly (sync_status field, asyncio.run pattern, RedBeat registration)
