---
# Placeholder — replace with production playbook
- name: Upgrade Kubernetes cluster
  hosts: all
  become: true
  vars:
    target_version: ""
    skip_preflight: false
  tasks:
    - name: Assert target_version is set
      ansible.builtin.assert:
        that: target_version != ""
        msg: "target_version must be provided"

    - name: Print upgrade plan
      ansible.builtin.debug:
        msg: "Would upgrade to {{ target_version }} (skip_preflight={{ skip_preflight }})"
